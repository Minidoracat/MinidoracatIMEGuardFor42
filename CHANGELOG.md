# Changelog

<!-- 撰寫規則摘要（完整版見 AGENTS.md「CHANGELOG 撰寫規則」）：
  - bullet 寫給玩家，會整段照貼 Workshop 更新說明：症狀先行、遊戲內名詞、
    禁檔名/函式名/行號/引擎術語；影響版本誠實寫清楚
  - 「> 技術要點：」（選用）只放管理員/modder 需要的行為事實；單次變更的完整
    技術原因寫在 commit message 本文（綁 diff、可 git log 搜尋、永不公開）
  - 紅線：不寫攻擊配方（安全修正只寫「強化了驗證」）、不寫玩家名/座標/Steam ID、
    不寫主機名/路徑/IP -->

所有重要的變更都會記錄在此檔案中。

格式基於 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.1.0/)，版本號遵循 `{PZ版本}-{主版本}.{次版本}.{修訂}` 格式。

## [42.20.4-0.1.5] - 2026-09-24

只更新配套工具，MOD 本體無變更。

### 改善

- **更新工具不必先關舊版**：舊版還在系統匣時直接開新版，新版會請舊版退出後接手，開機自動啟動也會改成啟動新版。以前新版會提示「已在執行」後自己結束，實際上仍在用舊版。這次從 0.1.4 升級時舊版還不支援，會提示你先從系統匣結束舊版。
- **下載檔名帶版本號**：例如 `pz-ime-guard-0.1.5.exe`，瀏覽器不會再存成「pz-ime-guard (1).exe」，也看得出是哪一版。舊版 exe 可以自行刪除。

> 技術要點：執行中的一份會在狀態目錄寫 `running.txt`（版本號）並建立具名事件；較新版據此請它正常退出。

## [42.20.4-0.1.4] - 2026-09-22

本版同時更新 MOD 與配套工具。請讓 Steam 更新 MOD，並下載新版 `pz-ime-guard.exe`；正常退出時提前停止守護需要兩者配合。

### 新增

- **登入 Windows 時自動啟動**：新增系統匣右鍵選項，預設關閉，由玩家自行選擇。取消勾選即可停用；工具搬到其他資料夾後，請手動執行一次以更新啟動位置。

### 修正

- **確認退出後，遊戲仍在收尾時繼續被切成英文**：現在確認退出到桌面後會停止守護，待切到其他視窗時嘗試還原原本的輸入法。回主選單不受影響；按視窗關閉、崩潰或強制結束仍依視窗消失處理。
- **0.1.3 切出遊戲後仍偶爾停在英文**：現在等待前景視窗穩定，確認還原結果並有限重試；暫停或關閉還原選項後，不會再補送先前的還原。也修正了暫停跨越遊戲重啟時，舊退出訊號可能讓新一局停止守護的問題。

## [42.20.4-0.1.3] - 2026-09-18

只更新配套工具（`pz-ime-guard.exe`），MOD 本體無變更、Workshop 不需更新。

### 修正

- **切出遊戲還原輸入法有時失效、之後一直卡英文**：按住 Alt+Tab 慢慢選視窗、或在遊戲和其他程式間切得很快時，還原可能送錯對象或沒送；更糟的是工具會把「桌面本來就是英文」誤記下來，之後每次切出去都不再還原。現在離開遊戲後會等到真正的目標視窗出現再還原（最多等 2 秒），而且被帶成英文的狀態永遠不會被當成「你本來的輸入法」。

> 技術要點：桌面配置只記非安全配置的值；離開 PZ 後進入待還原狀態，遇到前景執行緒配置＝安全配置才送 `WM_INPUTLANGCHANGEREQUEST`，Alt+Tab 切換器等過渡前景先跳過。實測對照：工具關閉時真實 Alt+Tab 後既有視窗與新程序都變 0x0409，開啟後三種情境（切換器久按、快速進出 ×3、前景中關遊戲）皆維持 0x0404。

## [42.20.4-0.1.2] - 2026-09-17

### 新增

- **切出遊戲時還原桌面的輸入法**：以前 alt-tab 出去或關掉遊戲後，之後新開的程式會停在英文鍵盤。現在離開 PZ 的瞬間會把你進遊戲前桌面用的輸入法還回去；右鍵選單可取消勾選（預設開）。桌面本來就是英文、或你有開「讓我為每個應用程式視窗設定不同的輸入法」的話，本來就不受影響，也不會多動。

> 技術要點：工具只還原自己造成的變動——PZ 不在前景時記住前景視窗的配置，離開 PZ 那一輪若新前景配置正是工具切過去的安全配置才送 `WM_INPUTLANGCHANGEREQUEST` 還原。偵測靠 100 ms 輪詢，最多慢一輪。

## [42.20.4-0.1.1] - 2026-09-17

### 新增

- **配套工具右鍵選單**：新增「關於（版本）」「Workshop 頁面」「GitHub」三項，點了直接開頁面。
- **自動檢查更新**：啟動時與每天查一次 GitHub 有沒有新版工具，有就問你要不要開下載頁；右鍵選單可取消勾選（預設開）。

### 修正

- **配套工具不會重複開兩份**：再點一次只會提示「已在執行中」並自動關閉那一份，不會出現兩個系統匣圖示。

## [42.20.4-0.1.0] - 2026-09-17

### 新增

- **初始版本**：玩遊戲時自動把 PZ 切到英文鍵盤，打字（聊天、搜尋、命名）時自動切回你原本的輸入法，注音、拼音、日文、韓文輸入法不再卡住移動與操作鍵。需搭配 Windows 配套工具 `pz-ime-guard.exe`（GitHub Releases 下載）；進遊戲時工具沒在跑會提醒一次。純 client 端，單人／多人皆可用。

> 技術要點：MOD 只寫「正在打字」旗標到 `Zomboid/Lua/MinidoracatIMEGuard/state.txt`，切換由工具以 `WM_INPUTLANGCHANGEREQUEST` 對 PZ 視窗執行並回讀確認；工具每 2 秒寫 `heartbeat.txt`。
